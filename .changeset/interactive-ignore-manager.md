---
"projector": minor
---

Add interactive `projector ignore` command with checkbox interface for managing project ignore patterns. Features smart pattern generation (suffix, prefix, path-based), pattern validation, dry-run mode, and automatic deduplication. Users can select/deselect projects visually and the system generates optimized glob patterns.
