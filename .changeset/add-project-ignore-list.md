---
"projector": minor
---

Add project ignore list system with glob patterns and .projectorignore file support. Users can now filter projects using global patterns in config.yaml and per-directory ignore files with glob syntax and negation support.

